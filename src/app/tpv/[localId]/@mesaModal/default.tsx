// Nada que mostrar mientras no se esté interceptando /mesa/[mesaId] — se
// pide en cada recarga y en cada carga inicial de cualquier página de este
// local, para las que este slot no tiene una subruta activa que recordar.
export default function Default() {
  return null;
}
